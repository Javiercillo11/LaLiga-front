/* describe('Componente principal de Angular', () => {
  beforeEach(() => {
    // Asegúrate de que el servidor Angular esté corriendo en esta URL
    cy.visit('http://localhost:4200/');
  });

  it('debería mostrar el texto principal', () => {
    cy.contains('¡Hola desde Angular!').should('be.visible');
  });

  it('debería mostrar el botón', () => {
    cy.get('button').should('contain.text', 'Haz clic aquí');
  });
  
}); */
